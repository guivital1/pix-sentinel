version = 0.1

[default.deploy.parameters]
stack_name = "pix-sentinel-dev"
resolve_s3 = true
region = "us-east-2"
capabilities = "CAPABILITY_IAM"
confirm_changeset = true
parameter_overrides = "EnableSimulation=false"
tags = "project=pix-sentinel environment=dev owner=guivital1"

